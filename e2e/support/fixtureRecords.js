// Records from fixtures/corpus that specs open by title, so they don't
// depend on list order. No spec may delete these: the delete spec creates
// and deletes its own record instead.
export const STABLE_RAW_SESSION_TITLE = 'Contextual Inquiry — Emergency Department Intake Shadowing';
